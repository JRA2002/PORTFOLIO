---
import GitHub from "./icons/GitHub.astro"
import NextJS from "./icons/NextJS.astro"
import Tailwind from "./icons/Tailwind.astro"
import Link from "./icons/Link.astro"
import LinkButton from "./LinkButton.astro"
import Python from "./icons/Python.astro"

const TAGS = {
  NEXT: {
    name: "Next.js",
    class: "bg-black text-white",
    icon: NextJS,
  },
  TAILWIND: {
    name: "Tailwind CSS",
    class: "bg-[#003159] text-white",
    icon: Tailwind,
  },
  PYTHON: {
    name: "Python",
    class: "bg-[#003159] text-white",
    icon: Python,
  },

}
const PROJECTS = [
{
  title: "SmartHome Manager - Aplicación web para la gestión de propiedades",
  description: `
- Backend construido con FastAPI y PostgreSQL, orquestado y desplegado en contenedores Docker.

- Frontend creado con Next.js y Tailwind CSS, desplegado en Vercel.

- Gestión completa de propiedades, alquileres y gastos.

- OCR con IA para leer y procesar facturas automáticamente.

- Chatbot inteligente integrado para asistencia y consultas dentro de la plataforma.
  `,
  link: "https://smarthome-manager.vercel.app/",
  github: "https://github.com/JRA2002/SMARTHOME_MANAGER",
  image: "/projects/smarthome.png",
  tags: [TAGS.NEXT, TAGS.PYTHON]
}
,
  {
    title: "TinderPet - Conoce a tu mascota ideal",
    description: `
• Frontend desarrollado con Next.js y Tailwind CSS (deploy en Vercel).

• Backend construido con Django y MySQL, orquestado y desplegado en contenedores Docker.

• Autenticación segura usando JWT.

• Sistema de “match” estilo Tinder para conectar adoptantes con mascotas compatibles.
`,

    link: "https://tinderpet-app.vercel.app/",
    github: "https://github.com/JRA2002/TINDERPET_APP",
    image: "/projects/tinder1.png",
    tags: [TAGS.NEXT, TAGS.TAILWIND, TAGS.PYTHON]
  },
]
---

<div class="grid grid-cols-1 md:grid-cols-2 gap-16">
  {
    PROJECTS.map(({ image, title, description, tags, link, github }) => (
    <article class="flex flex-col space-y-8 group">
      <div class="w-full">
        <div class="relative flex flex-col items-center gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
          <img alt={title} class="object-cover object-top w-full h-56 transition duration-500 md:group-hover:scale-105" loading="lazy" src={image} />
        </div>
      </div>

      <div class="w-full">
        <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">
          {title}
        </h3>
        <div class="flex flex-wrap mt-2">
          <ul class="flex flex-row mb-2 gap-x-2">
            {tags.map((tag) => (
              <li>
                <span class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2 `}>
                  <tag.icon class="size-4" />
                  {tag.name}
                </span>
              </li>
            ))}
          </ul>

          <div class="mt-2 text-gray-700 dark:text-gray-400 whitespace-pre-line">{description}</div>
          <footer class="flex items-end justify-start mt-4 gap-x-4">
            {github && (
              <LinkButton href={github}>
                <GitHub class="size-6" />
                Code
              </LinkButton>
            )}
            {link && (
              <LinkButton href={link}>
                <Link class="size-4" />
                Preview
              </LinkButton>
            )}
          </footer>
        </div>
      </div>
    </article>
    ))
  }
</div>




